= semantic_form_for [:admin, resource] do |f|
  = f.semantic_errors *f.object.errors.keys
  = f.inputs do
    ~ f.input :description
  = f.submit data: { disable_with: '请稍等...' }

:javascript
    $(function() {
        PkEditor.call($("#platform_question_description"), $('form.platform_question'));
    });


